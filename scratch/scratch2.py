import torch
# from torch.utils.data import DataLoader
#
# a = torch.ones(size=(10, 20, 30, 10))
# print(type(a))
# assert isinstance(a, torch.Tensor)
# b = a.cuda()
# print(type(b))

print(torch.cuda.is_available())
